# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-sources-assignment' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for ios-sources-assignment
  pod 'Kingfisher' 

  target 'ios-sources-assignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
